package com.framework.selenium;

public enum ExecutionMode {

	Local("Local", 0), Remote("Remote", 1), Perfecto("Perfecto", 3), Grid("Grid", 2);

	private ExecutionMode(final String name, final int ordinal) {

	}

}
